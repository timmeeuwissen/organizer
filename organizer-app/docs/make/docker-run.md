# `make docker-run`

Runs the `organizer-app` image on port 3000 (`docker run -p 3000:3000 organizer-app`).

Build first with `make docker-build`.

## Stop

Run `make docker-stop` to stop containers using the `organizer-app` image.
